{% extends "base.html.twig" %}

{% block title %}Modifier l'article "{{ post.title }}" &middot; {{ parent() }}{% endblock %}

{% block body %}

    <h1>Modifier l'article "{{ post.title }}"</h1>
    {% include 'admin/_post.html.twig' with {
        'url': path('admin_blog_edit', {'id': post.id}),
        'post': post
    } %}

{% endblock %}

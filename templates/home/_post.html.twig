<article class="blog-post">
    <h2 class="blog-post-title mb-1">
        <a href="{{ path('post', {'slug': post.slug}) }}" class="link-light">{{ post.title }}</a>
    </h2>
    <p class="blog-post-meta">
        {{ post.updatedAt|date("d/m/Y") }} par {{ post.author.username }}
        &middot;
        <small class="fst-italic">{{ post.comments }} commentaire{{ post.comments > 1 ? 's' }}</small>
    </p>
    <div class="text-justify">
        <p class="lead">{{ post.lede|nl2br }}</p>
        <p>{{ post.content|nl2br }}</p>
    </div>
</article>

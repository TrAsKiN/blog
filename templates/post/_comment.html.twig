<div>
    <div class="d-flex justify-content-between px-2">
        <p class="fw-bold mb-0">{{ comment.author }}</p>
        <small class="text-secondary fw-normal">{{ comment.createdAt|date("d/m/Y") }}</small>
    </div>
    <p class="py-2 px-3 rounded bg-opacity-10 bg-secondary">{{ comment.content|nl2br }}</p>
</div>
